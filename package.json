{
  "name": "root",
  "private": true,
  "version": "independent",
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "scripts": {
    "install": "lerna bootstrap",
    "build": "lerna run build",
    "deploy": "lerna publish --no-push --force-publish --no-git-reset"
  }
}
