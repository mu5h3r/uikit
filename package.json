{
  "name": "root",
  "private": true,
  "version": "independent",
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "scripts": {
    "install": "lerna bootstrap",
    "build": "lerna run build"
  }
}
